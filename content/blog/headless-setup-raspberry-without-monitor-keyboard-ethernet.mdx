---
title: (WIP) How to set RaspberryPi up without monitor, keyboard and ethernet cable
description:
keywords:
path: /blog/how-to-set-raspberrypi-up-without-monitor-keyboard-and-ethernet-cable
date: 2020-03-27
published: false
images:
    - author: liz west
      image: /assets/blog/covers/225231412_3e321057d4_k.jpg
      sourceText: Flickr
      source: https://www.flickr.com/photos/calliope/
      is_cover: 1
      galleryId: 0
---

https://www.raspberrypi.org/downloads/raspbian/

https://etcher.io/

```bash
cd /Volumes/boot;
touch ssh;
```

wpa_supplicant.conf:

```editorconfig
ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1
country=<TWO_LETTER_ISO_COUNTRY_CODE>

network={
    ssid="<WIFI_NETWORK_NAME>"
    psk="<WIFI_PASSWORD>"
    key_mgmt=WPA-PSK
}
```

```bash
ifconfig;
brew install nmap;
nmap -sn <IP>/24
```

pass: `raspberry`

```bash
ssh pi@<RPI_IP_ADDRESS>
```

```bash
mkdir .ssh;
cat ~/id_rsa.pub >> ~/.ssh/authorized_keys;
rm ~/id_rsa.pub;
sudo vi /etc/ssh/sshd_config;
```

PasswordAuthentication -> no

```bash
sudo service sshd restart;
```

```bash
sudo apt-get update
sudo apt-get dist-upgrade
```

## Node

```bash
curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.35.3/install.sh | bash;
source ~/.bashrc;
nvm install 13;
nvm use 13;
nvm alias default 13;
```

## Yarn

```bash
curl -sS https://dl.yarnpkg.com/debian/pubkey.gpg | sudo apt-key add -
echo "deb https://dl.yarnpkg.com/debian/ stable main" | sudo tee /etc/apt/sources.list.d/yarn.list
sudo apt update
sudo apt install --no-install-recommends yarn
```

## Docker

```bash
curl -fsSL https://get.docker.com -o get-docker.sh;
sudo sh get-docker.sh;
rm get-docker.sh;
sudo usermod -aG docker `whoami`;
docker version;
```

## Docker compose

Since Pi's processor is ARM, the [official installation guide](https://docs.docker.com/compose/install/) will not work.

```bash
sudo apt-get install -y libffi-dev libssl-dev python3 python3-pip;
sudo apt-get remove python-configparser;
sudo pip3 install docker-compose;
```
